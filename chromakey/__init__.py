"""chromakey — a stdlib-only design-token compiler.

One JSON in, every framework out: CSS, SCSS, Tailwind, and flat JSON,
with WCAG-graded contrast and a self-contained preview specimen.
No third-party dependencies; Python 3.9+.
"""

__version__ = "1.0.0"
